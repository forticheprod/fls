name: test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest
    container: doubleailes/rust-docker-nextest:0.1.1

    steps:
    - name: Bench
      run: cargo bench